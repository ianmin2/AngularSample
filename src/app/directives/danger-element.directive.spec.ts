import { DangerElementDirective } from './danger-element.directive';

describe('DangerElementDirective', () => {
  it('should create an instance', () => {
    const directive = new DangerElementDirective();
    expect(directive).toBeTruthy();
  });
});
