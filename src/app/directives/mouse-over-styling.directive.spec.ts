import { MouseOverStylingDirective } from './mouse-over-styling.directive';

describe('MouseOverStylingDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseOverStylingDirective();
    expect(directive).toBeTruthy();
  });
});
