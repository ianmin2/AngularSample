import { LiteDropdownDirective } from './lite-dropdown.directive';

describe('LiteDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new LiteDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
